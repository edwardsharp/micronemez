<h1>Listing nodes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Date</th>
    <th>Content</th>
    <th>Tags</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nodes.each do |node| %>
  <tr>
    <td><%= node.name %></td>
    <td><%= node.date %></td>
    <td><%= node.content %></td>
    <td><%= node.tags %></td>
    <td><%= link_to 'show', node %></td>
    <td><%= link_to 'edit', edit_node_path(node) %></td>
    <td><%= link_to 'destroy!', node, confirm: 'o rly?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Node', new_node_path %>
