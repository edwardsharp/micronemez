

	<div class="scrollableArea">
		<% @videos.each do |video| %>
			<%= link_to image_tag("/images/#{video.catnum}.jpg", :class=> 'video_thumb', :height => "330"), "/video/show/#{video.catnum}", :title => "#{video.title}",  :id => "#{video.catnum}" %>
      
		<% end %>
	</div>

