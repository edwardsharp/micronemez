
<div class="scrollWrapper scroll-pane horizontal-only">
	<div class="scrollableArea">
		<% @videos.each do |video| %>
			<%= link_to image_tag("#{video.thumbnailUrl}", :class=> 'video_thumb', :height => '330'), "#{video.thumbnailUrl}" %>

		<% end %>
	</div>
</div>
