

	<div class="scrollableArea">
		<% @videos.each do |video| %>
			<%= link_to image_tag("#{video.thumbnailUrl}", :class=> 'video_thumb', :height => '330'), "#{video.thumbnailUrl}", :title => "#{video.name}",  :id => "#{video.mediaId}" %>

		<% end %>
	</div>

