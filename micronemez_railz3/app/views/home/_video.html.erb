

	<div class="scrollableArea">
		<% @videos.each do |video| %>
			<%= link_to image_tag("#{video.thumbnailUrl}", :class=> 'video_thumb', :height => '330'), "/video/show/#{video.mediaId}", :title => "#{video.name}",  :id => "#{video.mediaId}" %>

		<% end %>
	</div>

